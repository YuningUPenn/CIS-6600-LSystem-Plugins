REMINDER:
The CMakeLists.txt has been modified to fit my own computer, with my Houdini in disk D instead of disk C. Modification is needed for this part. Also, build and rebuild can correctly generate the hlsystem.dll directly into the dso folder as long as the output path has the correct setting, but it cannot be also generated in the Release folder at the same time. I have manually put the .dll file into the folder for purpose of handing in assignment, but I need to remind again that this is not initially generated by .sln directly into the folder, which also leads to the solution build and rebuild fails for the last step (copying from Release fails as the files cannot be found, as it is not generated there). It is normal if the solution fails to build or rebuild, as it should only stuck on the last step of copying the files, while the .dll file should have been generated in the dso folder at that moment, which may also allow the plugin working correctly in Houdini.

For Assignment:
I have done all the three parts of the homework. The Houdini and Visual Studio are both correctly set up, and I have done all the "PUT YOUR CODE HERE" part for creating the personal Houdini L-System Node, reading a file for grammar and generating L-System base on that represented by white lines, and users may also change parameters to modify it. For extra credits, I add a Start Position parameter that controls the root position of the L-System, and users may input the xyz components of the start position they want to modify the position of the L-System. I also add an input node that allow users to link one type of basic polygon they want and then generate the branches with the chosen polygon. For this part, I do expect the users to reference the step of the L-System to the y-component or the height or any similar component in the input polygon in order to have the best performance. I have also set restrictions on the value of step and angle: users may drag to select step between 0.0001 and 10, and may also manually input values greater than 10, but they cannot have step less than 0.0001; users are also able to drag to select angle between 0 and 180, which is an restricted range that users cannot input an value out of the range.